#include "common.h"

int main(int argc, const char *argv[])
{
#if 0
	if (argc <4)
	{
		return fission(argv[0], -1, -1,SW_HIDE);
		//system("pause");
		//return n;
	}
	int s=atoi(argv[1]), a=atoi(argv[2]),show = atoi(argv[3]);
	return   fission(argv[0], s, a,show);
	//return n;
#endif
}